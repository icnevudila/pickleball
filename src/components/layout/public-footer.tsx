export function PublicFooter() {
  return (
    <footer className="border-t border-[color:var(--line)] py-10">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-4 px-4 text-sm text-[color:var(--muted)] sm:px-6 lg:flex-row lg:items-center lg:justify-between lg:px-8">
        <p>Guest-friendly reservations, faster desk control, and a public liveboard that stays readable.</p>
        <p>Warm public booking, clear member access, and staff operations on the same product spine.</p>
      </div>
    </footer>
  );
}
